{
  "name": "spindle-plot",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "tsc & cpx \"./src/**/*.{html,png,jpg,svg,js,mp4,css}\" ./dist/",
    "watch": "tsc --watch & cpx --watch \"./src/**/*.{html,png,jpg,svg,js,mp4,css}\" ./dist/ & http-server --no-cache"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "http-server": "^14.1.1",
    "typescript": "^5.2.2"
  }
}
